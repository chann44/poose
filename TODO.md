1. CLI parser to parse cli commands
2. config parser to parse config and load things
3. a parser to parse the sql 
4. runner to run commands
5. tui to see which migrations are applied and which one are not 
6. publish to pypi so i can use it zoo